name: Simple CI

on: [push, pull_request]

jobs:
  greetings:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Print Hello World
      run: echo Hello, world!

    - name: Another Simple Command
      run: echo Running another simple command...
